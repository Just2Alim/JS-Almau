# Lab 8.2: Event Delegation

**Benefits:** Instead of O(n) event listeners, we use O(1). This reduces memory consumption and automatically handles dynamically created DOM elements without needing to bind new event listeners.
